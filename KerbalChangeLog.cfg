KERBALCHANGELOG
{
	showChangelog = True
	modName = Civilian Population
	license = CC-BY
	author = michaelhester07, rabidninjawombat, GGumby, trafalgar, Pamynx, pmborg, Linuxgurugamer (current)
  
  	VERSION
	{
		version = 3.0.1.2
		CHANGE
		{
			change = Updated version file to use github.com for new version info
			change = Copied web page to README-WEB.md
		}

	}

	VERSION
	{
		version = 3.0.1.1
		CHANGE
		{
			change = Bugfix
			subchange = Fixed Invalid Operation Exception, was causing Kerbals to die upon entering a vessel
		}

	}

  	VERSION
	{
		version = 3.0.1
		CHANGE
		{
			change = Bugfix
			subchange = Fixed bug which would create a new kerbal when a kerbal went eva from anywhere on the same vessel
		}

	}

  	VERSION
	{
		version = 3.0.0.9
		CHANGE
		{
			change = Bugfix
			subchange = Fixed nullref when clicking the time button
			subchange = Fixed nullref when playing in Sandbox during FixedUpdate while trying to update fu
		}
		CHANGE
		{
			change = Updated part
			subchange = Thanks to user @pmborg for this: Added ModuleAsteroidDrill and ModuleCometDrill into the Netherdyne Laser Drill
		}

	}


	VERSION //Node(s) required for anything to show up
	{
		verison = 3.0.0.8
		//versionName = Audacious Apple //optional
		CHANGE //required in order to utilize subchanges
		{
			change = Merged all four projects into single project
			subchange = Add new change nodes //optional, as many as you want
			subchange = Add license field
			subchange = Add author node
			subchange = Add version naming field
		}
		CHANGE
		{
			change = Merged all four projects into single project 
		}
		CHANGE
		{
			change = Added back old parts, FlightSchool, Theater
		}
		CHANGE
		{
			change = Added back resources: education, flightExperience, inspiration
		}
		CHANGE
		{
			change = Fixed issue with nullrefs because of null vessels
		}
		CHANGE
		{
			change = Fixed issue with reloading from JSON, vessel wouldn't be properly linked
		}
		CHANGE
		{
			change = Numerous other small fixes
		}
		CHANGE
		{
			change = Eliminated unnecessary 2nd button, incorporated into single window with tabs
		}
	}

	

	//VERSION
	//{
	//	version = 1.1.7
	//	CHANGE
	//	{
	//		change = Update for KSP 1.9
	//	}
	//	CHANGE
	//	{
	//		change = Downgrade to .NET 4.5 for compatibility
	//	}
	//}
}